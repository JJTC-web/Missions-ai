SECRET_KEY=change-me-in-production
PORT=5000
# Local dev without Postgres falls back to a SQLite file at DATABASE_PATH.
# In production, set DATABASE_URL to a Postgres connection string and
# submissions persist across redeploys instead of living on local disk.
DATABASE_PATH=missionos.db
DATABASE_URL=
ANTHROPIC_API_KEY=
RESEND_API_KEY=
RESEND_FROM_EMAIL=onboarding@resend.dev
ADMIN_NOTIFICATION_EMAIL=ladyem34@gmail.com
TEST_EMAIL_TOKEN=
# Admin dashboard login (see README "Admin Dashboard" section for setup)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
